1. 我是一个馒头店老板，老板端需要登录验证。我通常提前一天发布明天要销售的品种。然后客户选择预定数量，提交订单。
第二天我一边售卖馒头，一边预留客户预定的订单，我需要快速查看每个品种是否留够或者多了。
然后不时需要清点还有多少没有取走的订单，检查待取和我的库存数量是否匹配。
预定总览根据数量降序排序，订单列表把待取排在前面，已取走下沉。
就是有效信息在前面。
2. 客户不需要登录，但需要填名称，微信id或电话，进入就可以看到可售卖的产品, 可以直接选择预定数量，提交订单。


用户预定页面顶部分区重新设计，整体就时间+设置入口。
    1. 显示说明:
    背景色为纯白 (#FFFFFF)。
    左侧时间选择： 取消传统的边框方框，采用‘大标题 + 下拉箭头’的形式。例如：‘周三 10月25日’使用粗体 (Semi-bold, 18px)，旁边跟一个极简的 Chevron 下拉图标。
    右侧设置入口： 使用细线条 (Thin line) 的圆圈包裹一个 Gear 图标，背景色为极淡的灰色 (#F5F5F5) 或透明。设置交互不动。
    细节： 底部不要有分割线，而是使用非常淡的投影 (Box-shadow: 0 4px 10px rgba(0,0,0,0.02))。整体风格对齐 iOS 原生设计。
    2. 交互逻辑设计：
2.1. 时间默认状态（静态显示）
    顶部只显示当前选中的日期，文字保持高级感。
    顶部时间区默认显示为：[周三 10月25日] + [Chevron图标]。文字居左或居中，字体使用 font-weight: 500，不加任何背景框，保持背景纯白。”
2.2. 时间点击触发：横向日期条（Inline Calendar）
点击那个日期文字后，下方平滑展开一行横向滑动的日期选择器。
    详细说明：
    “当用户点击顶部日期时，在下方以 Slide-down 动画展开一个横向滚动的日期列表（Horizontal Scroll）。
        样式： 每一项包含‘周几’和‘日期’，例如上方小字‘周四’，下方大字‘26’。
        选中态： 选中的日期使用一个正圆黑底白字，或者极简的下划线标记。
        非选中态： 文字使用浅灰色 (#999)。



1. 顶部操作区：全局状态监控 (The Status Bar)

这个区域要让店长一眼看到“今天还要做多少个馒头”。
“沿用方案 1 的 Header。顶部左侧显示当前日期。右侧保留设置入口（可切换回用户端）。
    新增‘实时看板’： 在 Header 下方设计一个极简的横向数据条。
        内容： 待取件：24 笔 | 待制作：120 个。
        样式： 使用中号、粗体数字。背景纯白，不加边线，底部留出一像素的浅灰色分割线。”

2. 核心区 A：核销流水线 (Real-time Fulfillment)

这是店长用得最多的地方。重点是“一键点击，实时反馈”。
    “设计一个‘订单’列表。
        卡片设计： 每个订单是一个简洁的长方形卡片，背景微灰 (#F9F9F9)，圆角 12px。
        信息层级： 左侧是用户昵称 + 手机后四位（大字）；中间是产品简述（如：白馒头x5, 杂粮x2）；右侧是一个巨大的**‘点击取走’**按钮，点击变成'重置'按钮。

        交互细节： 按钮初始为白色背景、细黑边框。点击后，按钮瞬间变为绿色对勾图标，并伴随一个向右滑出消失的动画（表示已核销）。”

3. 核心区 B：品类汇总 (Inventory Summary)

店长需要根据这个决定这一锅蒸什么。

    AI 指令：
    “在核销列表上方，增加一个可折叠的汇总面板。

        功能： 汇总显示今日所有订单的品类总量（如：全麦馒头 总计 50 个，剩余未取 20 个）。

        样式： 采用标签云 (Tag Cloud) 布局，每个品类是一个小方块，用深浅不同的颜色区分热销度。”

4. 底部功能区：计划与资源 (Planning & Resources)

将“发布菜单”和“产品池”这种非实时操作放在底部或二级页面。

    AI 指令：
    “设计一个吸底的导航栏（Tab Bar），仅包含四个极简图标：

        核销 (默认页)：即上述的实时订单列表。

        周菜单 (Calendar Icon)：点击进入‘一周发布’界面。

            布局： 左侧是周一到周日的竖向标签，右侧是对应日期的勾选列表，支持‘一键复制昨日菜单’。

        产品池 (Box Icon)：点击进入馒头品种管理，仅显示缩略图 + 价格 + 开关状态。

        数据 (Chart Icon)：查看历史订单和销售曲线。”

架构师的“店长端”避坑指南（发给你的 AI 助手）：

    防止误点： “点击取走”按钮虽然要大，但要增加一个**“长按”或“二次确认”**的逻辑，防止店长在忙乱中手指划过时误点。

    视觉减负： 如果店长端有 100 个订单，页面会很长。要求 AI 实现 “虚拟列表 (Virtual List)” 技术，确保滑动不卡顿。

    色彩心理： * 待处理：用沉稳的黑色或深蓝。

        已完成：用极淡的灰色或淡绿。

        千万不要用大红大绿，长时间盯着看会视觉疲劳。


