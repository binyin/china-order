我是一个馒头店老板，
1. 用户和老板用同一个小程序，默认是用户界面，顶部店店主，进入店主页面，老板输入手机号，密码登录，勾选今日品类(参考./test.data中的品类)。也有编辑模版可增删改品类和价格。
2. 默认是用户页面，无需登录，用户端字体大些，都用极简风格。默认今日可预定品种(图片，名称，价格，库存, 预定数)，用户端滑动窗口选择数量。顶部有总金额，提交和取消，取消订单不消失，进入取消状态。
3. 店主端3个功能，1. 编辑产品池，2. 当前可预订产品，可以从历史发布里选择再发布。3. 当前订单和历史订单，按日期分类。
4. 用户提交订单后，老板收到新订单提示，今日订单处理店主端request.md中的功能需求一致。
4. 服务端用微信托管服务，根据用户端和店主端需求建立必要的数据库集合。提供增删改查接口。