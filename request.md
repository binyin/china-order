# 馒头店点单核销助手 Pro - 需求与架构设计方案

## 1. 项目背景
解决馒头店日常经营中接龙混乱、手动核销效率低、库存统计难的问题。通过微信小程序原生开发，实现老板单手核销与用户极简点单的闭环。

---

## 2. 功能模块划分

### M1: 用户点单模块 (User Interface)
* **极简视觉**：采用大字体、高对比度设计，方便中老年或忙碌用户操作。
* **滑动计数**：大尺寸 `[-] 数量 [+]` 步进器，支持快速加减。
* **状态常驻**：底部显示实时总金额、提交及取消按钮。
* **订单追踪**：提交后显示订单状态（待取/已完成/已取消），取消后订单不消失，标记为置灰状态。

### M2: 老板管理模块 (Admin Interface)
* **登录系统**：手机号+密码鉴权，区分店主与普通用户界面。
* **产品池管理**：支持品类的增删改查（名称、单价、单位、图片）。
* **发布中心**：从产品池勾选今日售卖品种，设定库存，一键发布。
* **动态看板 (区域1)**：
    * **实时统计**：各品类“总预定”与“待取数”汇总。
    * **排序优化**：按“待取数量”降序排列，优先显示急需装袋的品种。
* **客户清单 (区域2)**：
    * **核销操作**：大按钮点击“取走”或“取消”。
    * **实时下沉**：已处理订单实时刷新至列表底部并置灰，待处理订单保持在顶部。

### M3: 财务统计模块 (Financial Stats)
* **实时营收**：自动计算全天预计总营收（待取+已取）。
* **实收统计**：实时展示当前实际已收款（仅限“已取走”订单）。

---

## 3. 技术架构说明

### 3.1 技术栈
* **前端**：微信小程序原生框架 (WXML / WXSS / JavaScript)。
* **后端**：微信云开发 (Cloud Development) - Serverless 架构。
* **数据库**：云数据库 (NoSQL) - 支持实时监听 (Watch)。
* **样式**：原子化 CSS 思想 + 微信小程序自定义组件。

### 3.2 数据库设计 (Schema)
* **`products`** (产品模板库)
    * `name`, `price`, `unit`, `image_url`
* **`active_menu`** (今日发布菜单)
    * `product_id`, `name`, `price`, `stock`, `date`
* **`orders`** (订单记录)
    * `customer_id`, `items[]`, `total_price`, `status` (pending/completed/cancelled), `create_time`

---

## 4. 核心交互流程

### 4.1 原子化库存扣减
用户提交订单时，调用云函数执行 `db.command.inc`，在数据库层面完成扣减，确保高并发下库存不超卖。

### 4.2 实时核销反馈
老板端利用 `db.collection('orders').watch()` 开启监听：
1.  **新单提醒**：数据库新增记录时，老板端自动弹出提示并刷新看板。
2.  **状态流转**：老板点击“取走”，订单 `status` 变更为 `completed`。
3.  **视觉动效**：通过小程序 `wx:if` 和列表排序逻辑，使订单卡片在 `pending` 区域消失，平滑出现在底部的 `history` 区域。

---

## 5. 设计原则 (UX)
* **Fitts's Law (菲茨定律)**：所有核销按钮操作区域高度不低于 50px，间距合理，满足单手拇指操作。
* **极简加载**：老板管理逻辑分包加载，确保用户端秒开。